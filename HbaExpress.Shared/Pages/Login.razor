@page "/login"
@layout BlankLayout
@inject ISellerApi Api
@inject AuthState Auth
@inject NavigationManager Nav

<PageTitle>Connexion — Espace Vendeur</PageTitle>

<div class="login-card">
    <div class="login-brand">
        <img class="login-logo" src="_content/HbaExpress.Shared/img/hbaexpress-logo.png" alt="HbaExpress Pro" />
    </div>
    <h1>Connexion</h1>
    <p class="mp-caption">Accédez à votre espace vendeur.</p>

    <EditForm Model="_login" OnValidSubmit="HandleLogin">
        <div class="field">
            <label for="email">E-mail</label>
            <InputText id="email" class="mp-input" @bind-Value="_login.Email" placeholder="vous@boutique.com" />
        </div>
        <div class="field">
            <label for="pwd">Mot de passe</label>
            <div class="pwd-wrap">
                <InputText id="pwd" type="@(_showPassword ? "text" : "password")" class="mp-input"
                           @bind-Value="_login.Password" placeholder="••••••••" />
                <button type="button" class="pwd-toggle" @onclick="TogglePassword"
                        aria-label="@(_showPassword ? "Masquer le mot de passe" : "Afficher le mot de passe")"
                        title="@(_showPassword ? "Masquer le mot de passe" : "Afficher le mot de passe")">
                    @if (_showPassword)
                    {
                        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 10 8 10 8a13.16 13.16 0 0 1-1.67 2.68"/><path d="M6.61 6.61A13.526 13.526 0 0 0 2 12s3 8 10 8a9.74 9.74 0 0 0 5.39-1.61"/><line x1="2" y1="2" x2="22" y2="22"/><path d="M14.12 14.12a3 3 0 1 1-4.24-4.24"/></svg>
                    }
                    else
                    {
                        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12s3-8 10-8 10 8 10 8-3 8-10 8-10-8-10-8Z"/><circle cx="12" cy="12" r="3"/></svg>
                    }
                </button>
            </div>
        </div>
        @* Champ MFA désactivé — décommenter ce bloc pour réactiver la double authentification.
        <div class="field">
            <label for="mfa">Code MFA <span class="mp-caption">(optionnel)</span></label>
            <InputText id="mfa" class="mp-input" @bind-Value="_login.MfaCode" placeholder="123456" />
        </div>
        *@

        @if (_error is not null)
        {
            <p class="login-error">@_error</p>
        }

        <MpButton Variant="ButtonVariant.Primary" Type="submit" FullWidth="true" Disabled="_busy">
            @(_busy ? "Connexion…" : "Se connecter")
        </MpButton>
    </EditForm>

    <p class="login-legal">
        En vous connectant, vous acceptez nos
        <a href="cgu">Conditions générales d'utilisation</a>.
    </p>
</div>

